[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Netlify's official Next.js runtime — handles serverless functions,
# Server Actions, and middleware. Auto-installed on deploy.
[[plugins]]
  package = "@netlify/plugin-nextjs"
